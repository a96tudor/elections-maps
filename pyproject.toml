[project]
name = "election_maps"
version = "0.1.0"
dependencies = [
    "pyutils @ git+https://github.com/a96tudor/pyutils.git",
    "flask==3.0.3",
    "boto3",
    "flask-cors==4.0.1",
    "certifi",
    "pymongo[srv]",
    "folium"
]


[tool.setuptools]
py-modules = ["election_maps", "scripts"]
